{
  "name": "base-utils",
  "version": "1.0.0",
  "scripts": {
    "upgrade": "pnpm update",
    "clear": "rimraf dist && rimraf node_modules",
    "lint": "eslint --ext .js,.ts --fix src"
  },
  "devDependencies": {
    "config-ts": "workspace:0.0.0"
  }
}
